```shell
cd ~/rstool/examples/serve

# default
rstool serve

# specific entrance
rstool serve -e entry.html

# specific port
rstool serve -p 80

# specific directory
cd ~/rstool/examples
rstool serve -r ./serve
```